version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /dev/null:/app/yarn.lock
      - /dev/null:/app/package-lock.json
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true